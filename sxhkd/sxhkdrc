# pkill -USR1 -x sxhkd
# launching applications
super + o; w
	firefox

super + o; d
	discord

super + o; m
	spotify

# media control
super + m; {space, Right, h, Left, l}
  playerctl -p spotify {play-pause, next, next, previous, previous}

