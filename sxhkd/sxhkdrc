# pkill -USR1 -x sxhkd
# launching applications
super + o; w
	firefox

super + o; d
	discord

super + o; m
	spotify

super + o; e
	gvim

super + o; r
	termite -e ranger

# media control
super + m; {space, Right, l, Left, h}
  playerctl -p spotify {play-pause, next, next, previous, previous}

